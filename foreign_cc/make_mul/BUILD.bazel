# Source tree of a tiny plain-Makefile project consumed by `make()`.
filegroup(
    name = "all_srcs",
    srcs = glob(["**"]),
    visibility = ["//foreign_cc:__pkg__"],
)

# Lets //buildifier:buildifier_test lint this file hermetically.
exports_files(["BUILD.bazel"])
